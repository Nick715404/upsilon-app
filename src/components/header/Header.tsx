import { Box, Typography } from "@mui/material"

type Props = {}

export default function Header({ }: Props) {
  return (
    <Box>
      <Typography>
        NICKLLLSON
      </Typography>
    </Box>
  )
}